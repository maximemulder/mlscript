:ctml

//│ Unit

1
//│ Int

()
//│ Unit

true
//│ Bool

1.2
//│ Decimal

(a) => (b) =>
    if a is
        Int    then (b as Int)
        String then (b as String)
//│ ⊤ → Int ⇒ β ≤ Int ∨ String ⇒ β ≤ String → Int ∨ String


"Yo" as String
//│ String

(x) => (x as Int)
//│ Int → Int
