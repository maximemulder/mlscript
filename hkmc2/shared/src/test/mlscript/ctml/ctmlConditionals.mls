:ctml

if true then 1 else ""
//│ Int

if false then 1 else ""
//│ Str

:fixme
if bool then 1 else ""
//│ ═══[ERROR] Cannot solve judgment ∀A, B. (True → A → (⊤ → A)) ∧ (False → ⊤ → (B → B)) ≤ Bool → π.
//│ Typing error tree:
//│   ∀A, B. (True → A → (⊤ → A)) ∧ (False → ⊤ → (B → B)) ≤ Bool → π
//│     ∀B. (True → ρ → (⊤ → ρ)) ∧ (False → ⊤ → (B → B)) ≤ Bool → π
//│       (True → ρ → (⊤ → ρ)) ∧ (False → ⊤ → (σ → σ)) ≤ Bool → π
//│         True → ρ → (⊤ → ρ) ≤ Bool → π
//│           Bool ≤ True
//│             False ≤ True
//│               ⊤ ≤ True
//│                 ⊤ ≤ ⊥
//│         False → ⊤ → (σ → σ) ≤ Bool → π
//│           Bool ≤ False
//│             True ≤ False
//│               True ≤ ⊥
//│                 ⊤ ≤ ⊥
// The right function should be decomposed into an intersection using bool bounds ?
