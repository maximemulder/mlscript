:ctml

fun id(x) = x
//│ id: ∀α. α → α

id(1)
//│ Int

id("Hello")
//│ Str

val idInt: [T extends Int] -> T -> Int = id
//│ FAILURE: Unexpected type error
//│ FAILURE LOCATION: raise (test.scala:62)
//│ ═══[ERROR] Cannot solve judgment ∀α. α → α ≤ ∀T. {T ≤ Int} ⟹ T → Int.
//│ Typing error tree:
//│   ∀α. α → α ≤ ∀T. {T ≤ Int} ⟹ T → Int
//│     ∀α. α → α ≤ {β ≤ Int} ⟹ β → Int
//│       γ → γ ≤ {β ≤ Int} ⟹ β → Int
//│         β ≤ Int
//│           ⊤ ≤ Int
//│             ⊤ ≤ ⊥

idInt(1)
//│ FAILURE: Unexpected type error
//│ FAILURE LOCATION: raise (test.scala:62)
//│ ═══[ERROR] Variable 'idInt' not found in the context.

:e
idInt("Hello")
//│ ═══[ERROR] Variable 'idInt' not found in the context.

:e
val idConflictingBounds: [A extends Int restricts Str] -> A -> A = id
//│ ═══[ERROR] Cannot solve judgment ∀α. α → α ≤ ∀A. {A ≤ Int, A ≥ Str} ⟹ A → A.
//│ Typing error tree:
//│   ∀α. α → α ≤ ∀A. {A ≤ Int, A ≥ Str} ⟹ A → A
//│     ∀α. α → α ≤ {β ≤ Int, β ≥ Str} ⟹ β → β
//│       γ → γ ≤ {β ≤ Int, β ≥ Str} ⟹ β → β
//│         β ≤ Int
//│           ⊤ ≤ Int
//│             ⊤ ≤ ⊥

val inc: [T extends Int] -> T -> Int = (x) => x + 1
//│ FAILURE: Unexpected type error
//│ FAILURE LOCATION: raise (test.scala:62)
//│ ═══[ERROR] Cannot solve judgment Int → Int ≤ ∀T. {T ≤ Int} ⟹ T → Int.
//│ Typing error tree:
//│   Int → Int ≤ ∀T. {T ≤ Int} ⟹ T → Int
//│     Int → Int ≤ {α ≤ Int} ⟹ α → Int
//│       α ≤ Int
//│         ⊤ ≤ Int
//│           ⊤ ≤ ⊥

inc(1)
//│ FAILURE: Unexpected type error
//│ FAILURE LOCATION: raise (test.scala:62)
//│ ═══[ERROR] Variable 'inc' not found in the context.

:e
inc("Hello")
//│ ═══[ERROR] Variable 'inc' not found in the context.

:e
val incError: [T extends Int] -> T -> Int = (x) => x ++ 1
//│ ═══[ERROR] Cannot solve judgment Str → Str ≤ Int → α.
//│ Typing error tree:
//│   Str → Str ≤ Int → α
//│     Int ≤ Str
//│       Int ≤ ⊥
//│         ⊤ ≤ ⊥
