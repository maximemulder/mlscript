:ctml

fun foo(a, b) =
    if a is
        Int then (b as Int)
        Str then (b as Str)
//│ foo: ∀δ ◁ {δ ≤ (Int ▷ {β ≤ Int}) ∨ (Str ▷ {β ≤ Str})}. (Int ∨ Str) → δ → (Int ∨ Str)

foo(1, 2)
//│ TYPE ERROR: Type variable 'δ' not found in the clauses.
//│ Typing error tree:
//│   δ → (Int ∨ Str) ≤ Int → β
//│     Int ≤ δ

6
//│ Int
