:ctml

// Tests copied from the `FcpConstrainedTypes2.mls` file.

val f(n) =
  n(0)
//│ f: ∀α, β. {α ≤ Int → β} ⟹ α → β

:te
f(top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α, β. {α ≤ Int → β} ⟹ α → β ≤ ⊤ → γ.
//│ Typing error tree:
//│   ∀α, β. {α ≤ Int → β} ⟹ α → β ≤ ⊤ → γ
//│     {δ ≤ Int → ε} ⟹ δ → ε ≤ ⊤ → γ
//│       δ → ε ≤ ⊤ → γ
//│         ⊤ ≤ δ
//│           ⊤ ≤ Int → ε

:te
f(top)(top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α, β. {α ≤ Int → β} ⟹ α → β ≤ ⊤ → γ.
//│ Typing error tree:
//│   ∀α, β. {α ≤ Int → β} ⟹ α → β ≤ ⊤ → γ
//│     {δ ≤ Int → ε} ⟹ δ → ε ≤ ⊤ → γ
//│       δ → ε ≤ ⊤ → γ
//│         ⊤ ≤ δ
//│           ⊤ ≤ Int → ε

val f(n, m) =
  n(0)
//│ f: ∀α, β. α → ⊤ → ({α ≤ Int → β} ⟹ β)

:te
f(top, top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. {⊤ ≤ Int → α} ⟹ ⊤ → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. {⊤ ≤ Int → α} ⟹ ⊤ → α ≤ ⊤ → β
//│     {⊤ ≤ Int → γ} ⟹ ⊤ → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
f(top, top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. {⊤ ≤ Int → α} ⟹ ⊤ → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. {⊤ ≤ Int → α} ⟹ ⊤ → α ≤ ⊤ → β
//│     {⊤ ≤ Int → γ} ⟹ ⊤ → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
val test(extr) =
  let f = (n, m) => n(0)
  let _ = extr(f)
  f(0)
//│ test: ∀α. ⊤ → ({Int ≤ Int → α} ⟹ ⊤ → α)
//│ FAILURE: Unexpected lack of type error

:te
val test(extr, x) =
  let f = (n, m) => x(n(0))
  let _ = extr(f)
  f(0)
//│ test: ∀α, β. ⊤ → ({α ≤ (∀γ. {Int ≤ Int → γ} ⟹ γ) → β} ⟹ α → ⊤ → β)
//│ FAILURE: Unexpected lack of type error

:te
val test(extr, x) =
  let f = (n, m) => x(n(0))
  f(0)
//│ test: ∀α, β. ⊤ → ({α ≤ (∀γ. {Int ≤ Int → γ} ⟹ γ) → β} ⟹ α → ⊤ → β)
//│ FAILURE: Unexpected lack of type error

:te
val test(extr, x) =
  let f = (n, m) => n(x, 0)
  f(0)
//│ test: ∀α, β. ⊤ → ({Int ≤ α → β, β ≤ Int → ⊤} ⟹ α → ⊤ → ⊥)
//│ FAILURE: Unexpected lack of type error
