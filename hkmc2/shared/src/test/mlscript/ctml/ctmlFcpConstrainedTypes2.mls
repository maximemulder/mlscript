:ctml

// Tests copied from the `FcpConstrainedTypes2.mls` file.

val f(n) =
  n(0)
//│ f: ∀α. (Int → α) → α

:te
f(top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ ⊤ → β
//│     (Int → γ) → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
f(top)(top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ ⊤ → β
//│     (Int → γ) → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

val f(n, m) =
  n(0)
//│ f: ∀α. (Int → α) → ⊤ → α

:te
f(top, top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β
//│     (Int → γ) → ⊤ → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
f(top, top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β
//│     (Int → γ) → ⊤ → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
val test(extr) =
  let f = (n, m) => n(0)
  let _ = extr(f)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment α → β ≤ (∀γ. (Int → γ) → ⊤ → γ) → δ.
//│ Typing error tree:
//│   α → β ≤ (∀γ. (Int → γ) → ⊤ → γ) → δ
//│     ∀γ. (Int → γ) → ⊤ → γ ≤ α
//│       ∀γ. (Int → γ) → ⊤ → γ ≤ Int → β
//│         (Int → ε) → ⊤ → ε ≤ Int → β
//│           Int ≤ Int → ε

:te
val test(extr, x) =
  let f = (n, m) => x(n(0))
  let _ = extr(f)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment α → β ≤ (γ → δ → ε) → ζ.
//│ Typing error tree:
//│   α → β ≤ (γ → δ → ε) → ζ
//│     γ → δ → ε ≤ α
//│       γ → δ → ε ≤ Int → β
//│         Int ≤ γ
//│           Int ≤ Int → η

:te
val test(extr, x) =
  let f = (n, m) => x(n(0))
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ (β → γ → δ) → ε.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ (β → γ → δ) → ε
//│     (Int → ζ) → ζ ≤ (β → γ → δ) → ε
//│       β → γ → δ ≤ Int → ζ
//│         Int ≤ β
//│           Int ≤ Int → η

:te
val test(extr, x) =
  let f = (n, m) => n(x, 0)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ ((β → Int → ⊤) → ⊤ → ⊥) → γ.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ ((β → Int → ⊤) → ⊤ → ⊥) → γ
//│     (Int → δ) → δ ≤ ((β → Int → ⊤) → ⊤ → ⊥) → γ
//│       (β → Int → ⊤) → ⊤ → ⊥ ≤ Int → δ
//│         Int ≤ β → Int → ⊤
