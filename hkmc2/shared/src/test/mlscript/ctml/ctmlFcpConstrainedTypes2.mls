:ctml

// Tests copied from the `FcpConstrainedTypes2.mls` file.

val f(n) =
  n(0)
//│ f: ∀α. (Int → α) → α

:te
f(top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ ⊤ → β
//│     (Int → γ) → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
f(top)(top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ ⊤ → β
//│     (Int → γ) → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

val f(n, m) =
  n(0)
//│ f: ∀α. (Int → α) → ⊤ → α

:te
f(top, top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β
//│     (Int → γ) → ⊤ → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
f(top, top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β
//│     (Int → γ) → ⊤ → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
val test(extr) =
  let f = (n, m) => n(0)
  let _ = extr(f)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. ((Int → α) ∧ β) → α ≤ (∀γ. (Int → γ) → ⊤ → γ) → δ.
//│ Typing error tree:
//│   ∀α. ((Int → α) ∧ β) → α ≤ (∀γ. (Int → γ) → ⊤ → γ) → δ
//│     ((Int → ε) ∧ β) → ε ≤ (∀γ. (Int → γ) → ⊤ → γ) → δ
//│       ∀γ. (Int → γ) → ⊤ → γ ≤ (Int → ε) ∧ β
//│         ∀γ. (Int → γ) → ⊤ → γ ≤ Int → ε
//│           (Int → ζ) → ⊤ → ζ ≤ Int → ε
//│             Int ≤ Int → ζ

:te
val test(extr, x) =
  let f = (n, m) => x(n(0))
  let _ = extr(f)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. ((Int → α) ∧ β) → α ≤ ((Int → γ) → ⊤ → δ) → ε.
//│ Typing error tree:
//│   ∀α. ((Int → α) ∧ β) → α ≤ ((Int → γ) → ⊤ → δ) → ε
//│     ((Int → ζ) ∧ β) → ζ ≤ ((Int → γ) → ⊤ → δ) → ε
//│       (Int → γ) → ⊤ → δ ≤ (Int → ζ) ∧ β
//│         (Int → γ) → ⊤ → δ ≤ Int → ζ
//│           Int ≤ Int → γ

:te
val test(extr, x) =
  let f = (n, m) => x(n(0))
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ ((Int → β) → ⊤ → γ) → δ.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ ((Int → β) → ⊤ → γ) → δ
//│     (Int → ε) → ε ≤ ((Int → β) → ⊤ → γ) → δ
//│       (Int → β) → ⊤ → γ ≤ Int → ε
//│         Int ≤ Int → β

:te
val test(extr, x) =
  let f = (n, m) => n(x, 0)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ (∀β. (γ → Int → β) → ⊤ → β) → δ.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ (∀β. (γ → Int → β) → ⊤ → β) → δ
//│     (Int → ε) → ε ≤ (∀β. (γ → Int → β) → ⊤ → β) → δ
//│       ∀β. (γ → Int → β) → ⊤ → β ≤ Int → ε
//│         (γ → Int → ζ) → ⊤ → ζ ≤ Int → ε
//│           Int ≤ γ → Int → ζ
