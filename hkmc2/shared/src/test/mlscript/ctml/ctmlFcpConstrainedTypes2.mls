:ctml

// Tests copied from the `FcpConstrainedTypes2.mls` file.

val f(n) =
  n(0)
//│ f: ∀α. (Int → α) → α

:te
f(top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ ⊤ → β
//│     (Int → γ) → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
f(top)(top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ ⊤ → β
//│     (Int → γ) → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

val f(n, m) =
  n(0)
//│ f: ∀α. (Int → α) → ⊤ → α

:te
f(top, top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β
//│     (Int → γ) → ⊤ → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
f(top, top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β
//│     (Int → γ) → ⊤ → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
val test(extr) =
  let f = (n, m) => n(0)
  let _ = extr(f)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α, β, γ, δ. {ε ≤ α → β, α ≤ Int → δ, δ ≤ γ} ⟹ α → γ ≤ (∀ζ. (Int → ζ) → ⊤ → ζ) → η.
//│ Typing error tree:
//│   ∀α, β, γ, δ. {ε ≤ α → β, α ≤ Int → δ, δ ≤ γ} ⟹ α → γ ≤ (∀ζ. (Int → ζ) → ⊤ → ζ) → η
//│     {ε ≤ θ → λ, θ ≤ Int → κ, κ ≤ ι} ⟹ θ → ι ≤ (∀ζ. (Int → ζ) → ⊤ → ζ) → η
//│       {θ ≤ Int → κ, κ ≤ ι} ⟹ θ → ι ≤ (∀ζ. (Int → ζ) → ⊤ → ζ) → η
//│         {κ ≤ ι} ⟹ θ → ι ≤ (∀ζ. (Int → ζ) → ⊤ → ζ) → η
//│           θ → ι ≤ (∀ζ. (Int → ζ) → ⊤ → ζ) → η
//│             ∀ζ. (Int → ζ) → ⊤ → ζ ≤ θ
//│               ∀ζ. (Int → ζ) → ⊤ → ζ ≤ Int → κ
//│                 (Int → μ) → ⊤ → μ ≤ Int → κ
//│                   Int ≤ Int → μ

:te
val test(extr, x) =
  let f = (n, m) => x(n(0))
  let _ = extr(f)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α, β, γ, δ. {ε ≤ α → β, α ≤ Int → δ, δ ≤ γ} ⟹ α → γ ≤ (∀ζ, η. {θ ≤ η → ζ} ⟹ (Int → η) → ⊤ → ζ) → ι.
//│ Typing error tree:
//│   ∀α, β, γ, δ. {ε ≤ α → β, α ≤ Int → δ, δ ≤ γ} ⟹ α → γ ≤ (∀ζ, η. {θ ≤ η → ζ} ⟹ (Int → η) → ⊤ → ζ) → ι
//│     {ε ≤ κ → ν, κ ≤ Int → μ, μ ≤ λ} ⟹ κ → λ ≤ (∀ζ, η. {θ ≤ η → ζ} ⟹ (Int → η) → ⊤ → ζ) → ι
//│       {κ ≤ Int → μ, μ ≤ λ} ⟹ κ → λ ≤ (∀ζ, η. {θ ≤ η → ζ} ⟹ (Int → η) → ⊤ → ζ) → ι
//│         {μ ≤ λ} ⟹ κ → λ ≤ (∀ζ, η. {θ ≤ η → ζ} ⟹ (Int → η) → ⊤ → ζ) → ι
//│           κ → λ ≤ (∀ζ, η. {θ ≤ η → ζ} ⟹ (Int → η) → ⊤ → ζ) → ι
//│             ∀ζ, η. {θ ≤ η → ζ} ⟹ (Int → η) → ⊤ → ζ ≤ κ
//│               ∀ζ, η. {θ ≤ η → ζ} ⟹ (Int → η) → ⊤ → ζ ≤ Int → μ
//│                 {θ ≤ ξ → ο} ⟹ (Int → ξ) → ⊤ → ο ≤ Int → μ
//│                   (Int → ξ) → ⊤ → ο ≤ Int → μ
//│                     Int ≤ Int → ξ

:te
val test(extr, x) =
  let f = (n, m) => x(n(0))
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ (∀β, γ. {δ ≤ γ → β} ⟹ (Int → γ) → ⊤ → β) → ε.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ (∀β, γ. {δ ≤ γ → β} ⟹ (Int → γ) → ⊤ → β) → ε
//│     (Int → ζ) → ζ ≤ (∀β, γ. {δ ≤ γ → β} ⟹ (Int → γ) → ⊤ → β) → ε
//│       ∀β, γ. {δ ≤ γ → β} ⟹ (Int → γ) → ⊤ → β ≤ Int → ζ
//│         {δ ≤ η → θ} ⟹ (Int → η) → ⊤ → θ ≤ Int → ζ
//│           (Int → η) → ⊤ → θ ≤ Int → ζ
//│             Int ≤ Int → η

:te
val test(extr, x) =
  let f = (n, m) => n(x, 0)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ (∀β. (γ → Int → β) → ⊤ → β) → δ.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ (∀β. (γ → Int → β) → ⊤ → β) → δ
//│     (Int → ε) → ε ≤ (∀β. (γ → Int → β) → ⊤ → β) → δ
//│       ∀β. (γ → Int → β) → ⊤ → β ≤ Int → ε
//│         (γ → Int → ζ) → ⊤ → ζ ≤ Int → ε
//│           Int ≤ γ → Int → ζ
