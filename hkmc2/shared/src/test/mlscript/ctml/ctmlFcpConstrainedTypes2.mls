:ctml

// Tests copied from the `FcpConstrainedTypes2.mls` file.

val f(n) =
  n(0)
//│ f: ∀α. (Int → α) → α

:te
f(top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ ⊤ → β
//│     (Int → γ) → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
f(top)(top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ ⊤ → β
//│     (Int → γ) → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

val f(n, m) =
  n(0)
//│ f: ∀α. (Int → α) → ⊤ → α

:te
f(top, top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β
//│     (Int → γ) → ⊤ → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
f(top, top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β
//│     (Int → γ) → ⊤ → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
val test(extr) =
  let f = (n, m) => n(0)
  let _ = extr(f)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α, β, γ, δ. {ε ≤ α → β, α ≤ Int → δ, δ ≤ γ} ⟹ α → γ ≤ (∀ζ. (Int → ζ) → ⊤ → ζ) → η.
//│ Typing error tree:
//│   ∀α, β, γ, δ. {ε ≤ α → β, α ≤ Int → δ, δ ≤ γ} ⟹ α → γ ≤ (∀ζ. (Int → ζ) → ⊤ → ζ) → η
//│     {ε ≤ θ → λ, θ ≤ Int → κ, κ ≤ ι} ⟹ θ → ι ≤ (∀ζ. (Int → ζ) → ⊤ → ζ) → η
//│       {θ ≤ Int → κ, κ ≤ ι} ⟹ θ → ι ≤ (∀ζ. (Int → ζ) → ⊤ → ζ) → η
//│         {κ ≤ ι} ⟹ θ → ι ≤ (∀ζ. (Int → ζ) → ⊤ → ζ) → η
//│           θ → ι ≤ (∀ζ. (Int → ζ) → ⊤ → ζ) → η
//│             ∀ζ. (Int → ζ) → ⊤ → ζ ≤ θ
//│               ∀ζ. (Int → ζ) → ⊤ → ζ ≤ Int → κ
//│                 (Int → μ) → ⊤ → μ ≤ Int → κ
//│                   Int ≤ Int → μ

:te
val test(extr, x) =
  let f = (n, m) => x(n(0))
  let _ = extr(f)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α, β, γ, δ. {ε ≤ α → β, α ≤ Int → δ, δ ≤ γ} ⟹ α → γ ≤ (∀ζ, η. {θ ≤ ζ → η} ⟹ (Int → ζ) → ⊤ → η) → ι.
//│ Typing error tree:
//│   ∀α, β, γ, δ. {ε ≤ α → β, α ≤ Int → δ, δ ≤ γ} ⟹ α → γ ≤ (∀ζ, η. {θ ≤ ζ → η} ⟹ (Int → ζ) → ⊤ → η) → ι
//│     {ε ≤ κ → ν, κ ≤ Int → μ, μ ≤ λ} ⟹ κ → λ ≤ (∀ζ, η. {θ ≤ ζ → η} ⟹ (Int → ζ) → ⊤ → η) → ι
//│       {κ ≤ Int → μ, μ ≤ λ} ⟹ κ → λ ≤ (∀ζ, η. {θ ≤ ζ → η} ⟹ (Int → ζ) → ⊤ → η) → ι
//│         {μ ≤ λ} ⟹ κ → λ ≤ (∀ζ, η. {θ ≤ ζ → η} ⟹ (Int → ζ) → ⊤ → η) → ι
//│           κ → λ ≤ (∀ζ, η. {θ ≤ ζ → η} ⟹ (Int → ζ) → ⊤ → η) → ι
//│             ∀ζ, η. {θ ≤ ζ → η} ⟹ (Int → ζ) → ⊤ → η ≤ κ
//│               ∀ζ, η. {θ ≤ ζ → η} ⟹ (Int → ζ) → ⊤ → η ≤ Int → μ
//│                 {θ ≤ ξ → ο} ⟹ (Int → ξ) → ⊤ → ο ≤ Int → μ
//│                   (Int → ξ) → ⊤ → ο ≤ Int → μ
//│                     Int ≤ Int → ξ

:te
val test(extr, x) =
  let f = (n, m) => x(n(0))
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ (∀β, γ. {δ ≤ β → γ} ⟹ (Int → β) → ⊤ → γ) → ε.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ (∀β, γ. {δ ≤ β → γ} ⟹ (Int → β) → ⊤ → γ) → ε
//│     (Int → ζ) → ζ ≤ (∀β, γ. {δ ≤ β → γ} ⟹ (Int → β) → ⊤ → γ) → ε
//│       ∀β, γ. {δ ≤ β → γ} ⟹ (Int → β) → ⊤ → γ ≤ Int → ζ
//│         {δ ≤ η → θ} ⟹ (Int → η) → ⊤ → θ ≤ Int → ζ
//│           (Int → η) → ⊤ → θ ≤ Int → ζ
//│             Int ≤ Int → η

:te
val test(extr, x) =
  let f = (n, m) => n(x, 0)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ (∀β. (γ → Int → β) → ⊤ → β) → δ.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ (∀β. (γ → Int → β) → ⊤ → β) → δ
//│     (Int → ε) → ε ≤ (∀β. (γ → Int → β) → ⊤ → β) → δ
//│       ∀β. (γ → Int → β) → ⊤ → β ≤ Int → ε
//│         (γ → Int → ζ) → ⊤ → ζ ≤ Int → ε
//│           Int ≤ γ → Int → ζ
