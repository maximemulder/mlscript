:ctml

// Tests copied from the `FcpConstrainedTypes2.mls` file.

val f(n) =
  n(0)
//│ f: ∀α. (Int → α) → α

:te
f(top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ ⊤ → β
//│     (Int → γ) → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
f(top)(top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ ⊤ → β
//│     (Int → γ) → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

val f(n, m) =
  n(0)
//│ f: ∀α. (Int → α) → ⊤ → α

:te
f(top, top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β
//│     (Int → γ) → ⊤ → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
f(top, top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β
//│     (Int → γ) → ⊤ → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
val test(extr) =
  let f = (n, m) => n(0)
  let _ = extr(f)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ (∀β. (Int → β) → ⊤ → β) → γ.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ (∀β. (Int → β) → ⊤ → β) → γ
//│     (Int → δ) → δ ≤ (∀β. (Int → β) → ⊤ → β) → γ
//│       ∀β. (Int → β) → ⊤ → β ≤ Int → δ
//│         (Int → ε) → ⊤ → ε ≤ Int → δ
//│           Int ≤ Int → ε

:te
val test(extr, x) =
  let f = (n, m) => x(n(0))
  let _ = extr(f)
  f(0)
//│ test: ∀α. ⊤ → ((∀β. {Int ≤ Int → β} ⟹ β) → α) → ⊤ → α
//│ FAILURE: Unexpected lack of type error

:te
val test(extr, x) =
  let f = (n, m) => x(n(0))
  f(0)
//│ test: ∀α. ⊤ → ((∀β. {Int ≤ Int → β} ⟹ β) → α) → ⊤ → α
//│ FAILURE: Unexpected lack of type error

:te
val test(extr, x) =
  let f = (n, m) => n(x, 0)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ (∀β. (γ → Int → β) → ⊤ → β) → δ.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ (∀β. (γ → Int → β) → ⊤ → β) → δ
//│     (Int → ε) → ε ≤ (∀β. (γ → Int → β) → ⊤ → β) → δ
//│       ∀β. (γ → Int → β) → ⊤ → β ≤ Int → ε
//│         (γ → Int → ζ) → ⊤ → ζ ≤ Int → ε
//│           Int ≤ γ → Int → ζ
