:ctml

// Tests copied from the `FcpConstrainedTypes2.mls` file.

val f(n) =
  n(0)
//│ f: ∀α, β. (Int → α) → ({β ≥ α} ⟹ β)

:te
f(top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α, β. (Int → α) → ({β ≥ α} ⟹ β) ≤ ⊤ → γ.
//│ Typing error tree:
//│   ∀α, β. (Int → α) → ({β ≥ α} ⟹ β) ≤ ⊤ → γ
//│     ∀β. (Int → δ) → ({β ≥ δ} ⟹ β) ≤ ⊤ → γ
//│       (Int → δ) → ({ε ≥ δ} ⟹ ε) ≤ ⊤ → γ
//│         ⊤ ≤ Int → δ

:te
f(top)(top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α, β. (Int → α) → ({β ≥ α} ⟹ β) ≤ ⊤ → γ.
//│ Typing error tree:
//│   ∀α, β. (Int → α) → ({β ≥ α} ⟹ β) ≤ ⊤ → γ
//│     ∀β. (Int → δ) → ({β ≥ δ} ⟹ β) ≤ ⊤ → γ
//│       (Int → δ) → ({ε ≥ δ} ⟹ ε) ≤ ⊤ → γ
//│         ⊤ ≤ Int → δ

val f(n, m) =
  n(0)
//│ f: ∀α, β. (Int → α) → ⊤ → ({β ≥ α} ⟹ β)

:te
f(top, top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α, β. (Int → α) → ⊤ → ({β ≥ α} ⟹ β) ≤ ⊤ → γ.
//│ Typing error tree:
//│   ∀α, β. (Int → α) → ⊤ → ({β ≥ α} ⟹ β) ≤ ⊤ → γ
//│     ∀β. (Int → δ) → ⊤ → ({β ≥ δ} ⟹ β) ≤ ⊤ → γ
//│       (Int → δ) → ⊤ → ({ε ≥ δ} ⟹ ε) ≤ ⊤ → γ
//│         ⊤ ≤ Int → δ

:te
f(top, top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α, β. (Int → α) → ⊤ → ({β ≥ α} ⟹ β) ≤ ⊤ → γ.
//│ Typing error tree:
//│   ∀α, β. (Int → α) → ⊤ → ({β ≥ α} ⟹ β) ≤ ⊤ → γ
//│     ∀β. (Int → δ) → ⊤ → ({β ≥ δ} ⟹ β) ≤ ⊤ → γ
//│       (Int → δ) → ⊤ → ({ε ≥ δ} ⟹ ε) ≤ ⊤ → γ
//│         ⊤ ≤ Int → δ

:te
val test(extr) =
  let f = (n, m) => n(0)
  let _ = extr(f)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α, β, γ, δ. {α ≤ (Int → β) ∧ ε, α ≤ Int → β} ⟹ α → ({γ ≥ {δ ≥ β} ⟹ δ} ⟹ γ) ≤ (∀ζ, η. (Int → ζ) → ⊤ → ({η ≥ ζ} ⟹ η)) → θ.
//│ Typing error tree:
//│   ∀α, β, γ, δ. {α ≤ (Int → β) ∧ ε, α ≤ Int → β} ⟹ α → ({γ ≥ {δ ≥ β} ⟹ δ} ⟹ γ) ≤ (∀ζ, η. (Int → ζ) → ⊤ → ({η ≥ ζ} ⟹ η)) → θ
//│     ∀β, γ, δ. {ι ≤ (Int → β) ∧ ε, ι ≤ Int → β} ⟹ ι → ({γ ≥ {δ ≥ β} ⟹ δ} ⟹ γ) ≤ (∀ζ, η. (Int → ζ) → ⊤ → ({η ≥ ζ} ⟹ η)) → θ
//│       ∀γ, δ. {ι ≤ (Int → κ) ∧ ε, ι ≤ Int → κ} ⟹ ι → ({γ ≥ {δ ≥ κ} ⟹ δ} ⟹ γ) ≤ (∀ζ, η. (Int → ζ) → ⊤ → ({η ≥ ζ} ⟹ η)) → θ
//│         ∀δ. {ι ≤ (Int → κ) ∧ ε, ι ≤ Int → κ} ⟹ ι → ({λ ≥ {δ ≥ κ} ⟹ δ} ⟹ λ) ≤ (∀ζ, η. (Int → ζ) → ⊤ → ({η ≥ ζ} ⟹ η)) → θ
//│           {ι ≤ (Int → κ) ∧ ε, ι ≤ Int → κ} ⟹ ι → ({λ ≥ {μ ≥ κ} ⟹ μ} ⟹ λ) ≤ (∀ζ, η. (Int → ζ) → ⊤ → ({η ≥ ζ} ⟹ η)) → θ
//│             {ι ≤ Int → κ} ⟹ ι → ({λ ≥ {μ ≥ κ} ⟹ μ} ⟹ λ) ≤ (∀ζ, η. (Int → ζ) → ⊤ → ({η ≥ ζ} ⟹ η)) → θ
//│               ι → ({λ ≥ {μ ≥ κ} ⟹ μ} ⟹ λ) ≤ (∀ζ, η. (Int → ζ) → ⊤ → ({η ≥ ζ} ⟹ η)) → θ
//│                 ∀ζ, η. (Int → ζ) → ⊤ → ({η ≥ ζ} ⟹ η) ≤ ι
//│                   ∀ζ, η. (Int → ζ) → ⊤ → ({η ≥ ζ} ⟹ η) ≤ ((Int → ν) ∧ ε) ∧ (Int → ξ)
//│                     ∀ζ, η. (Int → ζ) → ⊤ → ({η ≥ ζ} ⟹ η) ≤ (Int → ν) ∧ ε
//│                       ∀ζ, η. (Int → ζ) → ⊤ → ({η ≥ ζ} ⟹ η) ≤ Int → ν
//│                         ∀η. (Int → ο) → ⊤ → ({η ≥ ο} ⟹ η) ≤ Int → ν
//│                           (Int → ο) → ⊤ → ({π ≥ ο} ⟹ π) ≤ Int → ν
//│                             Int ≤ Int → ο

:te
val test(extr, x) =
  let f = (n, m) => x(n(0))
  let _ = extr(f)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α, β, γ, δ. {α ≤ (Int → β) ∧ ε, α ≤ Int → β} ⟹ α → ({γ ≥ {δ ≥ β} ⟹ δ} ⟹ γ) ≤ (∀ζ, η. {ζ ≤ ι} ⟹ (Int → ζ) → ⊤ → ({η ≥ θ} ⟹ η)) → κ.
//│ Typing error tree:
//│   ∀α, β, γ, δ. {α ≤ (Int → β) ∧ ε, α ≤ Int → β} ⟹ α → ({γ ≥ {δ ≥ β} ⟹ δ} ⟹ γ) ≤ (∀ζ, η. {ζ ≤ ι} ⟹ (Int → ζ) → ⊤ → ({η ≥ θ} ⟹ η)) → κ
//│     ∀β, γ, δ. {λ ≤ (Int → β) ∧ ε, λ ≤ Int → β} ⟹ λ → ({γ ≥ {δ ≥ β} ⟹ δ} ⟹ γ) ≤ (∀ζ, η. {ζ ≤ ι} ⟹ (Int → ζ) → ⊤ → ({η ≥ θ} ⟹ η)) → κ
//│       ∀γ, δ. {λ ≤ (Int → μ) ∧ ε, λ ≤ Int → μ} ⟹ λ → ({γ ≥ {δ ≥ μ} ⟹ δ} ⟹ γ) ≤ (∀ζ, η. {ζ ≤ ι} ⟹ (Int → ζ) → ⊤ → ({η ≥ θ} ⟹ η)) → κ
//│         ∀δ. {λ ≤ (Int → μ) ∧ ε, λ ≤ Int → μ} ⟹ λ → ({ν ≥ {δ ≥ μ} ⟹ δ} ⟹ ν) ≤ (∀ζ, η. {ζ ≤ ι} ⟹ (Int → ζ) → ⊤ → ({η ≥ θ} ⟹ η)) → κ
//│           {λ ≤ (Int → μ) ∧ ε, λ ≤ Int → μ} ⟹ λ → ({ν ≥ {ξ ≥ μ} ⟹ ξ} ⟹ ν) ≤ (∀ζ, η. {ζ ≤ ι} ⟹ (Int → ζ) → ⊤ → ({η ≥ θ} ⟹ η)) → κ
//│             {λ ≤ Int → μ} ⟹ λ → ({ν ≥ {ξ ≥ μ} ⟹ ξ} ⟹ ν) ≤ (∀ζ, η. {ζ ≤ ι} ⟹ (Int → ζ) → ⊤ → ({η ≥ θ} ⟹ η)) → κ
//│               λ → ({ν ≥ {ξ ≥ μ} ⟹ ξ} ⟹ ν) ≤ (∀ζ, η. {ζ ≤ ι} ⟹ (Int → ζ) → ⊤ → ({η ≥ θ} ⟹ η)) → κ
//│                 ∀ζ, η. {ζ ≤ ι} ⟹ (Int → ζ) → ⊤ → ({η ≥ θ} ⟹ η) ≤ λ
//│                   ∀ζ, η. {ζ ≤ ι} ⟹ (Int → ζ) → ⊤ → ({η ≥ θ} ⟹ η) ≤ ((Int → ο) ∧ ε) ∧ (Int → π)
//│                     ∀ζ, η. {ζ ≤ ι} ⟹ (Int → ζ) → ⊤ → ({η ≥ θ} ⟹ η) ≤ (Int → ο) ∧ ε
//│                       ∀ζ, η. {ζ ≤ ι} ⟹ (Int → ζ) → ⊤ → ({η ≥ θ} ⟹ η) ≤ Int → ο
//│                         ∀η. {ρ ≤ ι} ⟹ (Int → ρ) → ⊤ → ({η ≥ θ} ⟹ η) ≤ Int → ο
//│                           {ρ ≤ ι} ⟹ (Int → ρ) → ⊤ → ({σ ≥ θ} ⟹ σ) ≤ Int → ο
//│                             (Int → ρ) → ⊤ → ({σ ≥ θ} ⟹ σ) ≤ Int → ο
//│                               Int ≤ Int → ρ

:te
val test(extr, x) =
  let f = (n, m) => x(n(0))
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α, β. (Int → α) → ({β ≥ α} ⟹ β) ≤ (∀γ, δ. {γ ≤ ζ} ⟹ (Int → γ) → ⊤ → ({δ ≥ ε} ⟹ δ)) → η.
//│ Typing error tree:
//│   ∀α, β. (Int → α) → ({β ≥ α} ⟹ β) ≤ (∀γ, δ. {γ ≤ ζ} ⟹ (Int → γ) → ⊤ → ({δ ≥ ε} ⟹ δ)) → η
//│     ∀β. (Int → θ) → ({β ≥ θ} ⟹ β) ≤ (∀γ, δ. {γ ≤ ζ} ⟹ (Int → γ) → ⊤ → ({δ ≥ ε} ⟹ δ)) → η
//│       (Int → θ) → ({ι ≥ θ} ⟹ ι) ≤ (∀γ, δ. {γ ≤ ζ} ⟹ (Int → γ) → ⊤ → ({δ ≥ ε} ⟹ δ)) → η
//│         ∀γ, δ. {γ ≤ ζ} ⟹ (Int → γ) → ⊤ → ({δ ≥ ε} ⟹ δ) ≤ Int → θ
//│           ∀δ. {κ ≤ ζ} ⟹ (Int → κ) → ⊤ → ({δ ≥ ε} ⟹ δ) ≤ Int → θ
//│             {κ ≤ ζ} ⟹ (Int → κ) → ⊤ → ({λ ≥ ε} ⟹ λ) ≤ Int → θ
//│               (Int → κ) → ⊤ → ({λ ≥ ε} ⟹ λ) ≤ Int → θ
//│                 Int ≤ Int → κ

:te
val test(extr, x) =
  let f = (n, m) => n(x, 0)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α, β. (Int → α) → ({β ≥ α} ⟹ β) ≤ (∀γ, δ, ε, ζ, η. {δ ≤ Int → ε, γ ≤ θ → δ} ⟹ γ → ⊤ → ({η ≤ Int → ζ, ζ ≥ ε, η ≥ δ} ⟹ ζ)) → ι.
//│ Typing error tree:
//│   ∀α, β. (Int → α) → ({β ≥ α} ⟹ β) ≤ (∀γ, δ, ε, ζ, η. {δ ≤ Int → ε, γ ≤ θ → δ} ⟹ γ → ⊤ → ({η ≤ Int → ζ, ζ ≥ ε, η ≥ δ} ⟹ ζ)) → ι
//│     ∀β. (Int → κ) → ({β ≥ κ} ⟹ β) ≤ (∀γ, δ, ε, ζ, η. {δ ≤ Int → ε, γ ≤ θ → δ} ⟹ γ → ⊤ → ({η ≤ Int → ζ, ζ ≥ ε, η ≥ δ} ⟹ ζ)) → ι
//│       (Int → κ) → ({λ ≥ κ} ⟹ λ) ≤ (∀γ, δ, ε, ζ, η. {δ ≤ Int → ε, γ ≤ θ → δ} ⟹ γ → ⊤ → ({η ≤ Int → ζ, ζ ≥ ε, η ≥ δ} ⟹ ζ)) → ι
//│         ∀γ, δ, ε, ζ, η. {δ ≤ Int → ε, γ ≤ θ → δ} ⟹ γ → ⊤ → ({η ≤ Int → ζ, ζ ≥ ε, η ≥ δ} ⟹ ζ) ≤ Int → κ
//│           ∀δ, ε, ζ, η. {δ ≤ Int → ε, μ ≤ θ → δ} ⟹ μ → ⊤ → ({η ≤ Int → ζ, ζ ≥ ε, η ≥ δ} ⟹ ζ) ≤ Int → κ
//│             ∀ε, ζ, η. {ν ≤ Int → ε, μ ≤ θ → ν} ⟹ μ → ⊤ → ({η ≤ Int → ζ, ζ ≥ ε, η ≥ ν} ⟹ ζ) ≤ Int → κ
//│               ∀ζ, η. {ν ≤ Int → ξ, μ ≤ θ → ν} ⟹ μ → ⊤ → ({η ≤ Int → ζ, ζ ≥ ξ, η ≥ ν} ⟹ ζ) ≤ Int → κ
//│                 ∀η. {ν ≤ Int → ξ, μ ≤ θ → ν} ⟹ μ → ⊤ → ({η ≤ Int → ο, ο ≥ ξ, η ≥ ν} ⟹ ο) ≤ Int → κ
//│                   {ν ≤ Int → ξ, μ ≤ θ → ν} ⟹ μ → ⊤ → ({π ≤ Int → ο, ο ≥ ξ, π ≥ ν} ⟹ ο) ≤ Int → κ
//│                     {μ ≤ θ → ν} ⟹ μ → ⊤ → ({π ≤ Int → ο, ο ≥ ξ, π ≥ ν} ⟹ ο) ≤ Int → κ
//│                       μ → ⊤ → ({π ≤ Int → ο, ο ≥ ξ, π ≥ ν} ⟹ ο) ≤ Int → κ
//│                         Int ≤ μ
//│                           Int ≤ θ → ρ
