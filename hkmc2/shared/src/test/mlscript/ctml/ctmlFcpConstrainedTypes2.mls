:ctml

// Tests copied from the `FcpConstrainedTypes2.mls` file.

val f(n) =
  n(0)
//│ f: ∀α. (Int → α) → α

:te
f(top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ ⊤ → β
//│     (Int → γ) → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
f(top)(top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ ⊤ → β
//│     (Int → γ) → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

val f(n, m) =
  n(0)
//│ f: ∀α. (Int → α) → ⊤ → α

:te
f(top, top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β
//│     (Int → γ) → ⊤ → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
f(top, top)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β.
//│ Typing error tree:
//│   ∀α. (Int → α) → ⊤ → α ≤ ⊤ → β
//│     (Int → γ) → ⊤ → γ ≤ ⊤ → β
//│       ⊤ ≤ Int → γ

:te
val test(extr) =
  let f = (n, m) => n(0)
  let _ = extr(f)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α, β, γ, δ. {δ ≤ γ, α ≤ Int → δ, ε ≤ α → β} ⟹ α → γ ≤ (∀ζ, η. {ζ ≤ Int → η} ⟹ ζ → ⊤ → η) → θ.
//│ Typing error tree:
//│   ∀α, β, γ, δ. {δ ≤ γ, α ≤ Int → δ, ε ≤ α → β} ⟹ α → γ ≤ (∀ζ, η. {ζ ≤ Int → η} ⟹ ζ → ⊤ → η) → θ
//│     {μ ≤ κ, ι ≤ Int → μ, ε ≤ ι → λ} ⟹ ι → κ ≤ (∀ζ, η. {ζ ≤ Int → η} ⟹ ζ → ⊤ → η) → θ
//│       {ι ≤ Int → μ, ε ≤ ι → λ} ⟹ ι → κ ≤ (∀ζ, η. {ζ ≤ Int → η} ⟹ ζ → ⊤ → η) → θ
//│         {ε ≤ ι → λ} ⟹ ι → κ ≤ (∀ζ, η. {ζ ≤ Int → η} ⟹ ζ → ⊤ → η) → θ
//│           ι → κ ≤ (∀ζ, η. {ζ ≤ Int → η} ⟹ ζ → ⊤ → η) → θ
//│             ∀ζ, η. {ζ ≤ Int → η} ⟹ ζ → ⊤ → η ≤ ι
//│               ∀ζ, η. {ζ ≤ Int → η} ⟹ ζ → ⊤ → η ≤ Int → μ
//│                 {ν ≤ Int → ξ} ⟹ ν → ⊤ → ξ ≤ Int → μ
//│                   ν → ⊤ → ξ ≤ Int → μ
//│                     Int ≤ ν
//│                       Int ≤ Int → ξ

:te
val test(extr, x) =
  let f = (n, m) => x(n(0))
  let _ = extr(f)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α, β, γ, δ. {δ ≤ γ, α ≤ Int → δ, ε ≤ α → β} ⟹ α → γ ≤ (∀ζ, η, θ. {ζ ≤ Int → η, ι ≤ η → θ} ⟹ ζ → ⊤ → θ) → κ.
//│ Typing error tree:
//│   ∀α, β, γ, δ. {δ ≤ γ, α ≤ Int → δ, ε ≤ α → β} ⟹ α → γ ≤ (∀ζ, η, θ. {ζ ≤ Int → η, ι ≤ η → θ} ⟹ ζ → ⊤ → θ) → κ
//│     {ξ ≤ μ, λ ≤ Int → ξ, ε ≤ λ → ν} ⟹ λ → μ ≤ (∀ζ, η, θ. {ζ ≤ Int → η, ι ≤ η → θ} ⟹ ζ → ⊤ → θ) → κ
//│       {λ ≤ Int → ξ, ε ≤ λ → ν} ⟹ λ → μ ≤ (∀ζ, η, θ. {ζ ≤ Int → η, ι ≤ η → θ} ⟹ ζ → ⊤ → θ) → κ
//│         {ε ≤ λ → ν} ⟹ λ → μ ≤ (∀ζ, η, θ. {ζ ≤ Int → η, ι ≤ η → θ} ⟹ ζ → ⊤ → θ) → κ
//│           λ → μ ≤ (∀ζ, η, θ. {ζ ≤ Int → η, ι ≤ η → θ} ⟹ ζ → ⊤ → θ) → κ
//│             ∀ζ, η, θ. {ζ ≤ Int → η, ι ≤ η → θ} ⟹ ζ → ⊤ → θ ≤ λ
//│               ∀ζ, η, θ. {ζ ≤ Int → η, ι ≤ η → θ} ⟹ ζ → ⊤ → θ ≤ Int → ξ
//│                 {ο ≤ Int → ρ, ι ≤ ρ → π} ⟹ ο → ⊤ → π ≤ Int → ξ
//│                   {ι ≤ ρ → π} ⟹ ο → ⊤ → π ≤ Int → ξ
//│                     ο → ⊤ → π ≤ Int → ξ
//│                       Int ≤ ο
//│                         Int ≤ Int → ρ

:te
val test(extr, x) =
  let f = (n, m) => x(n(0))
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ (∀β, γ, δ. {β ≤ Int → γ, ε ≤ γ → δ} ⟹ β → ⊤ → δ) → ζ.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ (∀β, γ, δ. {β ≤ Int → γ, ε ≤ γ → δ} ⟹ β → ⊤ → δ) → ζ
//│     (Int → η) → η ≤ (∀β, γ, δ. {β ≤ Int → γ, ε ≤ γ → δ} ⟹ β → ⊤ → δ) → ζ
//│       ∀β, γ, δ. {β ≤ Int → γ, ε ≤ γ → δ} ⟹ β → ⊤ → δ ≤ Int → η
//│         {θ ≤ Int → κ, ε ≤ κ → ι} ⟹ θ → ⊤ → ι ≤ Int → η
//│           {ε ≤ κ → ι} ⟹ θ → ⊤ → ι ≤ Int → η
//│             θ → ⊤ → ι ≤ Int → η
//│               Int ≤ θ
//│                 Int ≤ Int → κ

:te
val test(extr, x) =
  let f = (n, m) => n(x, 0)
  f(0)
//│ ═══[TYPE ERROR] Cannot solve judgment ∀α. (Int → α) → α ≤ (∀β, γ. {β ≤ δ → Int → γ} ⟹ β → ⊤ → γ) → ε.
//│ Typing error tree:
//│   ∀α. (Int → α) → α ≤ (∀β, γ. {β ≤ δ → Int → γ} ⟹ β → ⊤ → γ) → ε
//│     (Int → ζ) → ζ ≤ (∀β, γ. {β ≤ δ → Int → γ} ⟹ β → ⊤ → γ) → ε
//│       ∀β, γ. {β ≤ δ → Int → γ} ⟹ β → ⊤ → γ ≤ Int → ζ
//│         {η ≤ δ → Int → θ} ⟹ η → ⊤ → θ ≤ Int → ζ
//│           η → ⊤ → θ ≤ Int → ζ
//│             Int ≤ η
//│               Int ≤ δ → Int → θ
