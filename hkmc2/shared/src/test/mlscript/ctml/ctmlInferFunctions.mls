:ctml

// Function intersections type inference

fun baz: (Int -> Int -> Int) & (Str -> Str -> Str)
//│ baz: (Int → Int → Int) ∧ (Str → Str → Str)

baz(1, 2)
//│ Int

baz("Hello", "World")
//│ Str

baz(bot, bot)
//│ ⊥

:te
baz(1, "World")
//│ ═══[TYPE ERROR] Cannot solve judgment α ≤ Str → β.
//│ Typing error tree:
//│   α ≤ Str → β
//│     Int → Int ≤ Str → β
//│       Str ≤ Int

:te
baz(1, top)
//│ ═══[TYPE ERROR] Cannot solve judgment α ≤ ⊤ → β.
//│ Typing error tree:
//│   α ≤ ⊤ → β
//│     Int → Int ≤ ⊤ → β
//│       ⊤ ≤ Int
