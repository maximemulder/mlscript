:ctml

fun idInt(x) = (x as Int)
//│ idInt: Int → Int

idInt(1)
//│ Int

fun id(x) = x
//│ id: ∀α ⇐ {α ≥ ⊥, α ≤ ⊤}. α → α

id(1)
//│ TYPE ERROR: Fail default case ∀α ⇐ {α ≥ ⊥, α ≤ ⊤}. α → α ≤ Int → α.

val a: Top = 1
//│ a: ⊤

()
//│ Unit
