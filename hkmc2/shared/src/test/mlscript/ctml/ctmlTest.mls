:ctml

""
//│ Str

:ctml-dbg var infer meet

fun foo(a, b) =
  if a is
    Int then (b as Int)
    Str then (b as Str)
//│ infer ELam(a,ELam(b,EMatch(EVar(a),List(EMatchCase(Int,EAscr(EVar(b),Int)), EMatchCase(Str,EAscr(EVar(b),Str))))))
//│   α fresh
//│   infer ELam(b,EMatch(EVar(a),List(EMatchCase(Int,EAscr(EVar(b),Int)), EMatchCase(Str,EAscr(EVar(b),Str)))))
//│     β fresh
//│     infer EMatch(EVar(a),List(EMatchCase(Int,EAscr(EVar(b),Int)), EMatchCase(Str,EAscr(EVar(b),Str))))
//│       infer EVar(a)
//│       OK α ⇝ ∅
//│       meet Int ∨ Str and ⊤
//│       = Int ∨ Str
//│       meet Int ∨ Str and ⊤
//│       = Int ∨ Str
//│       γ fresh
//│       meet Int ∨ Str and ⊤
//│       = Int ∨ Str
//│       meet Int and Int ∨ Str
//│       = Int
//│       meet Int ∨ Str and ⊤
//│       = Int ∨ Str
//│       meet Int and Int ∨ Str
//│       = Int
//│       infer EAscr(EVar(b),Int)
//│         infer EVar(b)
//│         OK β ⇝ ∅
//│         meet Int and ⊤
//│         = Int
//│       OK Int ⇝ β ≤ Int
//│       meet Int ∨ Str and ⊤
//│       = Int ∨ Str
//│       meet Int and Int ∨ Str
//│       = Int
//│       meet Int and ⊤
//│       = Int
//│       meet Int ∨ Str and ⊤
//│       = Int ∨ Str
//│       meet Str and Int ∨ Str
//│       = Str
//│       meet Int ∨ Str and ⊤
//│       = Int ∨ Str
//│       meet Str and Int ∨ Str
//│       = Str
//│       infer EAscr(EVar(b),Str)
//│         infer EVar(b)
//│         OK β ⇝ ∅
//│         meet Str and ⊤
//│         = Str
//│       OK Str ⇝ β ≤ Str
//│       meet Int ∨ Str and ⊤
//│       = Int ∨ Str
//│       meet Str and Int ∨ Str
//│       = Str
//│       meet Str and ⊤
//│       = Str
//│       meet Int and ⊤
//│       = Int
//│       meet Str and ⊤
//│       = Str
//│       meet Int and ⊤
//│       = Int
//│       meet Int and ⊤
//│       = Int
//│       meet Int ∨ Str and ⊤
//│       = Int ∨ Str
//│       meet Str and ⊤
//│       = Str
//│       meet Str and ⊤
//│       = Str
//│       meet Int ∨ Str and ⊤
//│       = Int ∨ Str
//│       meet Int and ⊤
//│       = Int
//│       meet Str and ⊤
//│       = Str
//│       meet Int and ⊤
//│       = Int
//│       meet Str and ⊤
//│       = Str
//│       meet Int ∨ Str and ⊤
//│       = Int ∨ Str
//│       meet Int and Int ∨ Str
//│       = Int
//│       meet Int ∨ Str and ⊤
//│       = Int ∨ Str
//│       meet Int and Int ∨ Str
//│       = Int
//│       meet Int ∨ Str and ⊤
//│       = Int ∨ Str
//│       meet Str and Int ∨ Str
//│       = Str
//│       meet Int ∨ Str and ⊤
//│       = Int ∨ Str
//│       meet Str and Int ∨ Str
//│       = Str
//│       meet Int and ⊤
//│       = Int
//│       meet Str and ⊤
//│       = Str
//│       meet Int ∨ Str and ⊤
//│       = Int ∨ Str
//│       meet Int ∨ Str and ⊤
//│       = Int ∨ Str
//│       meet Int and ⊤
//│       = Int
//│       meet Str and ⊤
//│       = Str
//│     OK γ ⇝ β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), γ fresh, α ≤ Int ∨ Str
//│   OK β → γ ⇝ β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), γ fresh, α ≤ Int ∨ Str, b: β, β fresh
//│   meet Int ∨ Str and ⊤
//│   = Int ∨ Str
//│   meet Int ∨ Str and ⊤
//│   = Int ∨ Str
//│   meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and Int ∨ Str
//│   = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│   meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│   meet Int ∨ Str and ⊤
//│   = Int ∨ Str
//│   meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and Int ∨ Str
//│   = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│   quantify α with ⊥ and (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│   = ∀α ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})}. α → β → γ
//│   meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│   meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│   quantify β with ⊥ and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│   = ∀α, β ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})}. α → β → γ
//│   quantify γ with (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}) and ⊤
//│   = ∀α, β, γ ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. α → β → γ
//│ OK ∀α, β, γ ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. α → β → γ ⇝ b: β, a: α
//│ foo: ∀α, β, γ ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. α → β → γ

foo(1, 2)
//│ infer EApp(EApp(EVar(foo),EVar(int)),EVar(int))
//│   δ fresh
//│   infer EApp(EVar(foo),EVar(int))
//│     ε fresh
//│     infer EVar(foo)
//│     OK ∀α, β, γ ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. α → β → γ ⇝ ∅
//│     infer EVar(int)
//│     OK Int ⇝ ∅
//│     γ fresh
//│     β fresh
//│     α fresh
//│     meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│     meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│     meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Int and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Int and Int ▷ {γ ≥ Int, α ≤ Int}
//│       = Int ▷ {γ ≥ Int, α ≤ Int}
//│       meet Int and Str ▷ {γ ≥ Str, α ≤ Str}
//│       = ⊥
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥
//│     meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│     meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Int and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Int and Int ▷ {γ ≥ Int, α ≤ Int}
//│       = Int ▷ {γ ≥ Int, α ≤ Int}
//│       meet Int and Str ▷ {γ ≥ Str, α ≤ Str}
//│       = ⊥
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥
//│     inline ε with β → γ
//│     = β → γ
//│     meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│     meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Int and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Int and Int ▷ {γ ≥ Int, α ≤ Int}
//│       = Int ▷ {γ ≥ Int, α ≤ Int}
//│       meet Int and Str ▷ {γ ≥ Str, α ≤ Str}
//│       = ⊥
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥
//│     meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│     quantify α with Int and (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│     = ∀α ◁ {α ≥ Int, α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})}. β → γ
//│     meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Int and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Int and Int ▷ {γ ≥ Int, α ≤ Int}
//│       = Int ▷ {γ ≥ Int, α ≤ Int}
//│       meet Int and Str ▷ {γ ≥ Str, α ≤ Str}
//│       = ⊥
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥
//│     meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Int and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Int and Int ▷ {γ ≥ Int, α ≤ Int}
//│       = Int ▷ {γ ≥ Int, α ≤ Int}
//│       meet Int and Str ▷ {γ ≥ Str, α ≤ Str}
//│       = ⊥
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥
//│     quantify β with ⊥ and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥
//│     = ∀α, β ◁ {α ≥ Int, α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥}. β → γ
//│     quantify γ with Int ∨ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}) and ⊤
//│     = ∀α, β, γ ◁ {α ≥ Int, α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥, γ ≥ Int ∨ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. β → γ
//│   OK ∀α, β, γ ◁ {α ≥ Int, α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥, γ ≥ Int ∨ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. β → γ ⇝ ∅
//│   infer EVar(int)
//│   OK Int ⇝ ∅
//│   γ fresh
//│   β fresh
//│   α fresh
//│   meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│   meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥ and ⊤
//│   = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥
//│   meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥ and ⊤
//│   = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥
//│   meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│   meet Int and (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│     meet Int and Int ▷ {γ ≥ Int, β ≤ Int}
//│     = Int ▷ {γ ≥ Int, β ≤ Int}
//│     meet Int and Str ▷ {γ ≥ Str, β ≤ Str}
//│     = ⊥
//│   = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ ⊥
//│   meet δ and ⊤
//│   = δ
//│   meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│   meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥ and ⊤
//│   = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥
//│   meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│   meet Int and (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│     meet Int and Int ▷ {γ ≥ Int, β ≤ Int}
//│     = Int ▷ {γ ≥ Int, β ≤ Int}
//│     meet Int and Str ▷ {γ ≥ Str, β ≤ Str}
//│     = ⊥
//│   = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ ⊥
//│   meet δ and ⊤
//│   = δ
//│   inline δ with Int ∨ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})
//│   = Int ∨ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})
//│   meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│   meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥ and ⊤
//│   = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥
//│   meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│   meet Int and (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│     meet Int and Int ▷ {γ ≥ Int, β ≤ Int}
//│     = Int ▷ {γ ≥ Int, β ≤ Int}
//│     meet Int and Str ▷ {γ ≥ Str, β ≤ Str}
//│     = ⊥
//│   = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ ⊥
//│   meet δ and ⊤
//│   = δ
//│   meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│   meet Int and (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│     meet Int and Int ▷ {γ ≥ Int, β ≤ Int}
//│     = Int ▷ {γ ≥ Int, β ≤ Int}
//│     meet Int and Str ▷ {γ ≥ Str, β ≤ Str}
//│     = ⊥
//│   = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ ⊥
//│   quantify α with Int and (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ ⊥
//│   = ∀α ◁ {α ≥ Int, α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ ⊥}. Int ∨ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})
//│   meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥ and ⊤
//│   = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥
//│   meet δ and ⊤
//│   = δ
//│   meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥ and ⊤
//│   = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥
//│   quantify β with Int and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥
//│   = ∀α, β ◁ {α ≥ Int, α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ ⊥, β ≥ Int, β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥}. Int ∨ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})
//│   meet δ and ⊤
//│   = δ
//│   meet δ and ⊤
//│   = δ
//│   inline γ with δ
//│   meet Int and ⊤
//│   = Int
//│   meet Str and ⊤
//│   = Str
//│   = ∀α, β ◁ {α ≥ Int, α ≤ (Int ▷ {β ≤ Int}) ∨ ⊥, β ≥ Int, β ≤ (Int ▷ {α ≤ Int}) ∨ ⊥}. Int ∨ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})
//│ OK ∀α, β ◁ {α ≥ Int, α ≤ (Int ▷ {β ≤ Int}) ∨ ⊥, β ≥ Int, β ≤ (Int ▷ {α ≤ Int}) ∨ ⊥}. Int ∨ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}) ⇝ ∅
//│ ∀α, β ◁ {α ≥ Int, α ≤ (Int ▷ {β ≤ Int}) ∨ ⊥, β ≥ Int, β ≤ (Int ▷ {α ≤ Int}) ∨ ⊥}. Int ∨ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})

fun bop(a, b) =
  let x = foo(a, b)
  x
//│ infer ELam(a,ELam(b,EApp(ELam(x,EVar(x)),EApp(EApp(EVar(foo),EVar(a)),EVar(b)))))
//│   ζ fresh
//│   infer ELam(b,EApp(ELam(x,EVar(x)),EApp(EApp(EVar(foo),EVar(a)),EVar(b))))
//│     η fresh
//│     infer EApp(ELam(x,EVar(x)),EApp(EApp(EVar(foo),EVar(a)),EVar(b)))
//│       θ fresh
//│       infer ELam(x,EVar(x))
//│         ι fresh
//│         infer EVar(x)
//│         OK ι ⇝ ∅
//│         quantify ι with ⊥ and ⊤
//│         = ∀ι ◁ {}. ι → ι
//│       OK ∀ι ◁ {}. ι → ι ⇝ x: ι
//│       infer EApp(EApp(EVar(foo),EVar(a)),EVar(b))
//│         κ fresh
//│         infer EApp(EVar(foo),EVar(a))
//│           λ fresh
//│           infer EVar(foo)
//│           OK ∀α, β, γ ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. α → β → γ ⇝ ∅
//│           infer EVar(a)
//│           OK ζ ⇝ ∅
//│           γ fresh
//│           β fresh
//│           α fresh
//│           meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│           = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│           meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│           = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│           meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│           = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│           meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│           = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│           meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│           = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│           meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│           = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│           meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│           = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│           meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│           = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│           inline λ with β → γ
//│           = β → γ
//│           meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│           = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│           meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│           = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│           meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│           = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│           meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│           = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│           meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│           = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│           ignore α
//│           = β → γ
//│         OK β → γ ⇝ ζ ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ fresh, β fresh
//│         meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│         = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│         = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│         infer EVar(b)
//│         OK η ⇝ ∅
//│         meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│         = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│         = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│         meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│         = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│         = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│         = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet κ and ⊤
//│         = κ
//│         meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│         = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│         = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│         meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│         = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet κ and ⊤
//│         = κ
//│         inline κ with (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})
//│         = (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})
//│       OK (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}) ⇝ γ ≤ κ, η ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), β ≥ η, ζ ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ fresh, β fresh
//│       meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│       meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet κ and ⊤
//│       = κ
//│       ι fresh
//│       meet Int and ⊤
//│       = Int
//│       meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Int and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet Int and Int ▷ {γ ≥ Int, α ≤ Int}
//│         = Int ▷ {γ ≥ Int, α ≤ Int}
//│         meet Int and Str ▷ {γ ≥ Str, α ≤ Str}
//│         = ⊥
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ ⊥
//│       meet θ and ⊤
//│       = θ
//│       ι fresh
//│       meet Str and ⊤
//│       = Str
//│       meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Str and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet Str and Int ▷ {γ ≥ Int, α ≤ Int}
//│         = ⊥
//│         meet Str and Str ▷ {γ ≥ Str, α ≤ Str}
//│         = Str ▷ {γ ≥ Str, α ≤ Str}
//│       = ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet θ and ⊤
//│       = θ
//│       meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│       meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet κ and ⊤
//│       = κ
//│       meet Str and ⊤
//│       = Str
//│       meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Str and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet Str and Int ▷ {γ ≥ Int, α ≤ Int}
//│         = ⊥
//│         meet Str and Str ▷ {γ ≥ Str, α ≤ Str}
//│         = Str ▷ {γ ≥ Str, α ≤ Str}
//│       = ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet θ and ⊤
//│       = θ
//│       meet Str and ⊤
//│       = Str
//│       meet Int and Str
//│       = ⊥
//│       meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Str and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet Str and Int ▷ {γ ≥ Int, α ≤ Int}
//│         = ⊥
//│         meet Str and Str ▷ {γ ≥ Str, α ≤ Str}
//│         = Str ▷ {γ ≥ Str, α ≤ Str}
//│       = ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Int and ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet Int and ⊥
//│         = ⊥
//│         meet Int and Str ▷ {γ ≥ Str, α ≤ Str}
//│         = ⊥
//│       = ⊥
//│       meet θ and ⊤
//│       = θ
//│       inline θ with Int ∨ Str
//│       = Int ∨ Str
//│       meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│       meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet κ and ⊤
//│       = κ
//│       meet Str and ⊤
//│       = Str
//│       meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Str and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet Str and Int ▷ {γ ≥ Int, α ≤ Int}
//│         = ⊥
//│         meet Str and Str ▷ {γ ≥ Str, α ≤ Str}
//│         = Str ▷ {γ ≥ Str, α ≤ Str}
//│       = ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet θ and ⊤
//│       = θ
//│       meet Str and ⊤
//│       = Str
//│       meet Int and Str
//│       = ⊥
//│       meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Str and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet Str and Int ▷ {γ ≥ Int, α ≤ Int}
//│         = ⊥
//│         meet Str and Str ▷ {γ ≥ Str, α ≤ Str}
//│         = Str ▷ {γ ≥ Str, α ≤ Str}
//│       = ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Int and ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet Int and ⊥
//│         = ⊥
//│         meet Int and Str ▷ {γ ≥ Str, α ≤ Str}
//│         = ⊥
//│       = ⊥
//│       meet θ and ⊤
//│       = θ
//│       meet θ and ⊤
//│       = θ
//│       ignore ι
//│       = Int ∨ Str
//│       meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│       meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet κ and ⊤
//│       = κ
//│       meet Str and ⊤
//│       = Str
//│       meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Str and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet Str and Int ▷ {γ ≥ Int, α ≤ Int}
//│         = ⊥
//│         meet Str and Str ▷ {γ ≥ Str, α ≤ Str}
//│         = Str ▷ {γ ≥ Str, α ≤ Str}
//│       = ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet θ and ⊤
//│       = θ
//│       meet Str and ⊤
//│       = Str
//│       meet Int and Str
//│       = ⊥
//│       meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│       = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Str and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet Str and Int ▷ {γ ≥ Int, α ≤ Int}
//│         = ⊥
//│         meet Str and Str ▷ {γ ≥ Str, α ≤ Str}
//│         = Str ▷ {γ ≥ Str, α ≤ Str}
//│       = ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Int and ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│         meet Int and ⊥
//│         = ⊥
//│         meet Int and Str ▷ {γ ≥ Str, α ≤ Str}
//│         = ⊥
//│       = ⊥
//│       meet θ and ⊤
//│       = θ
//│       ignore ι
//│       = Int ∨ Str
//│     OK Int ∨ Str ⇝ β ≤ Int, α ≤ Int, β ≤ Str, α ≤ Str, γ ≤ κ, η ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), β ≥ η, ζ ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ fresh, β fresh, x: ι
//│     meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│     meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet κ and ⊤
//│     = κ
//│     meet Str and ⊤
//│     = Str
//│     meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Str and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Str and Int ▷ {γ ≥ Int, α ≤ Int}
//│       = ⊥
//│       meet Str and Str ▷ {γ ≥ Str, α ≤ Str}
//│       = Str ▷ {γ ≥ Str, α ≤ Str}
//│     = ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Str and ⊤
//│     = Str
//│     meet Int and Str
//│     = ⊥
//│     meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Str and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Str and Int ▷ {γ ≥ Int, α ≤ Int}
//│       = ⊥
//│       meet Str and Str ▷ {γ ≥ Str, α ≤ Str}
//│       = Str ▷ {γ ≥ Str, α ≤ Str}
//│     = ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Int and ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│       meet Int and ⊥
//│       = ⊥
//│       meet Int and Str ▷ {γ ≥ Str, α ≤ Str}
//│       = ⊥
//│     = ⊥
//│     meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│     = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     inline η with (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     = ((Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})) → (Int ∨ Str)
//│   OK ((Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})) → (Int ∨ Str) ⇝ β ≤ Int, α ≤ Int, β ≤ Str, α ≤ Str, γ ≤ κ, β ≥ η, ζ ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ fresh, β fresh, x: ι, b: η
//│   meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│   meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│   meet κ and ⊤
//│   = κ
//│   meet Str and ⊤
//│   = Str
//│   meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│   meet Str and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Str and Int ▷ {γ ≥ Int, α ≤ Int}
//│     = ⊥
//│     meet Str and Str ▷ {γ ≥ Str, α ≤ Str}
//│     = Str ▷ {γ ≥ Str, α ≤ Str}
//│   = ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│   meet Str and ⊤
//│   = Str
//│   meet Int and Str
//│   = ⊥
//│   meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│   meet Str and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Str and Int ▷ {γ ≥ Int, α ≤ Int}
//│     = ⊥
//│     meet Str and Str ▷ {γ ≥ Str, α ≤ Str}
//│     = Str ▷ {γ ≥ Str, α ≤ Str}
//│   = ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│   meet Int and ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Int and ⊥
//│     = ⊥
//│     meet Int and Str ▷ {γ ≥ Str, α ≤ Str}
//│     = ⊥
//│   = ⊥
//│   meet (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│   inline ζ with (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//│   meet Str and ⊤
//│   = Str
//│   meet Int and Str
//│   = ⊥
//│   meet Str and ⊤
//│   = Str
//│   meet Int and Str
//│   = ⊥
//│   = ((Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})) → ((Int ▷ {γ ≥ Int}) ∨ (Str ▷ {γ ≥ Str})) → (Int ∨ Str)
//│   meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│   meet κ and ⊤
//│   = κ
//│   meet Str and ⊤
//│   = Str
//│   meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│   meet Str and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Str and Int ▷ {γ ≥ Int, α ≤ Int}
//│     = ⊥
//│     meet Str and Str ▷ {γ ≥ Str, α ≤ Str}
//│     = Str ▷ {γ ≥ Str, α ≤ Str}
//│   = ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│   meet Str and ⊤
//│   = Str
//│   meet Int and Str
//│   = ⊥
//│   meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│   meet Str and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Str and Int ▷ {γ ≥ Int, α ≤ Int}
//│     = ⊥
//│     meet Str and Str ▷ {γ ≥ Str, α ≤ Str}
//│     = Str ▷ {γ ≥ Str, α ≤ Str}
//│   = ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│   meet Int and ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Int and ⊥
//│     = ⊥
//│     meet Int and Str ▷ {γ ≥ Str, α ≤ Str}
//│     = ⊥
//│   = ⊥
//│   meet (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}) and ⊤
//│   = (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│   meet Str and (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Str and Int ▷ {γ ≥ Int, α ≤ Int}
//│     = ⊥
//│     meet Str and Str ▷ {γ ≥ Str, α ≤ Str}
//│     = Str ▷ {γ ≥ Str, α ≤ Str}
//│   = ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│   meet Int and ⊥ ∨ (Str ▷ {γ ≥ Str, α ≤ Str})
//│     meet Int and ⊥
//│     = ⊥
//│     meet Int and Str ▷ {γ ≥ Str, α ≤ Str}
//│     = ⊥
//│   = ⊥
//│   quantify β with η and ⊥
//│   = ∀β ◁ {β ≥ η, β ≤ ⊥}. ((Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})) → ((Int ▷ {γ ≥ Int}) ∨ (Str ▷ {γ ≥ Str})) → (Int ∨ Str)
//│   meet κ and ⊤
//│   = κ
//│   meet Str and ⊤
//│   = Str
//│   meet Str and ⊤
//│   = Str
//│   meet Int and Str
//│   = ⊥
//│   meet κ and ⊤
//│   = κ
//│   inline γ with κ
//│   meet Int and ⊤
//│   = Int
//│   meet Str and ⊤
//│   = Str
//│   = ∀β ◁ {β ≥ η, β ≤ ⊥}. ((Int ▷ {β ≤ Int}) ∨ (Str ▷ {β ≤ Str})) → (Int ∨ Str) → (Int ∨ Str)
//│ OK ∀β ◁ {β ≥ η, β ≤ ⊥}. ((Int ▷ {β ≤ Int}) ∨ (Str ▷ {β ≤ Str})) → (Int ∨ Str) → (Int ∨ Str) ⇝ α ≤ Int, α ≤ Str, x: ι, b: η, a: ζ
//│ bop: ∀β ◁ {β ≥ η, β ≤ ⊥}. ((Int ▷ {β ≤ Int}) ∨ (Str ▷ {β ≤ Str})) → (Int ∨ Str) → (Int ∨ Str)

// WIP: Problem is here:
//         infer EApp(EVar(foo),EVar(a))
//           ι fresh
//           infer EVar(foo)
//           OK ∀α, β, γ ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. α → β → γ ⇝ ∅
//           infer EVar(a)
//           OK δ ⇝ ∅
//           constrain ∀α, β, γ ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. α → β → γ ≤ δ → ι
//             γ fresh
//             β fresh
//             α fresh
//             constrain α → β → γ ≤ δ → ι
//               constrain δ ≤ α
//                 constrain δ ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//                   constrain ⊥ ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//                   OK ⇝ ∅
//                 OK ⇝ δ ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str})
//               OK ⇝ δ ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), α ≥ δ
//               constrain β → γ ≤ ι
//                 constrain β → γ ≤ ⊤
//                 OK ⇝ ∅
//               OK ⇝ ι ≥ β → γ
//             OK ⇝ ι ≥ β → γ, δ ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), α ≥ δ
//           OK ⇝ ι ≥ β → γ, δ ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), α ≥ δ, γ fresh, β fresh, α fresh
//           inline pos ι with β → γ
//           ignore α
//         WHERE DID β AND γ BOUNDS GO ???
//         OK β → γ ⇝ δ ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), γ fresh, β fresh
