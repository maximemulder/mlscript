:ctml

fun foo(a, b) =
  if a is
    Int then (b as Int)
    Str then (b as Str)
//│ γ Set(Str, Int)
//│ List() IN β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), γ fresh ||||| α ≤ Int ∨ Str, b: β, β fresh, a: α, α fresh, ++: Str → Str → Str, -: Int → Int → Int, +: Int → Int → Int, string: Str, num: Num, int: Int, bool: Bool, unit: Unit, bot: ⊥, top: ⊤, Str class, Num class, Int class, Bool class, Unit class, Bot class, Top class, untyped class, Function class, Object class
//│ γ Set(Str, Int)
//│ β Set(Str, Int)
//│ List() IN β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), γ fresh, α ≤ Int ∨ Str, b: β, β fresh ||||| a: α, α fresh, ++: Str → Str → Str, -: Int → Int → Int, +: Int → Int → Int, string: Str, num: Num, int: Int, bool: Bool, unit: Unit, bot: ⊥, top: ⊤, Str class, Num class, Int class, Bool class, Unit class, Bot class, Top class, untyped class, Function class, Object class
//│ γ Set(Str, Int)
//│ β Set(Str, Int)
//│ α Set(Str, Int)
//│ List(γ, β, α) IN β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), γ fresh, α ≤ Int ∨ Str, b: β, β fresh, a: α, α fresh ||||| ++: Str → Str → Str, -: Int → Int → Int, +: Int → Int → Int, string: Str, num: Num, int: Int, bool: Bool, unit: Unit, bot: ⊥, top: ⊤, Str class, Num class, Int class, Bool class, Unit class, Bot class, Top class, untyped class, Function class, Object class
//│ foo: ∀α, β, γ ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. α → β → γ

foo(1, 2)
//│ γ Set(Int)
//│ β Set(Int)
//│ α Set(Int, Str)
//│ ε Set(γ Set(Int), β Set(Int))
//│ List(γ, β, α, ε) IN ε ≥ β → γ, γ ≥ Int, β ≤ Int, α ≥ Int, γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), γ fresh, β fresh, α fresh, ε fresh ||||| δ fresh, foo: ∀α, β, γ ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. α → β → γ, ++: Str → Str → Str, -: Int → Int → Int, +: Int → Int → Int, string: Str, num: Num, int: Int, bool: Bool, unit: Unit, bot: ⊥, top: ⊤, Str class, Num class, Int class, Bool class, Unit class, Bot class, Top class, untyped class, Function class, Object class
//│ δ Set(Int)
//│ List(δ) IN δ ≥ Int, δ fresh ||||| foo: ∀α, β, γ ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. α → β → γ, ++: Str → Str → Str, -: Int → Int → Int, +: Int → Int → Int, string: Str, num: Num, int: Int, bool: Bool, unit: Unit, bot: ⊥, top: ⊤, Str class, Num class, Int class, Bool class, Unit class, Bot class, Top class, untyped class, Function class, Object class
//│ Int

fun bop(a, b) =
  let x = foo(a, b)
  x
//│ ι
//│ List(ι) IN x: ι, ι fresh ||||| θ fresh, b: η, η fresh, a: ζ, ζ fresh, foo: ∀α, β, γ ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. α → β → γ, ++: Str → Str → Str, -: Int → Int → Int, +: Int → Int → Int, string: Str, num: Num, int: Int, bool: Bool, unit: Unit, bot: ⊥, top: ⊤, Str class, Num class, Int class, Bool class, Unit class, Bot class, Top class, untyped class, Function class, Object class
//│ γ Set(Str, Int)
//│ β Set(Str, Int)
//│ α Set(ζ Set(Str, Int), Str, Int)
//│ λ Set(γ Set(Str, Int), β Set(Str, Int))
//│ List(λ) IN λ ≥ β → γ, ζ ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), α ≥ ζ, γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), γ fresh, β fresh, α fresh, λ fresh ||||| κ fresh, x: ι, θ fresh, b: η, η fresh, a: ζ, ζ fresh, foo: ∀α, β, γ ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. α → β → γ, ++: Str → Str → Str, -: Int → Int → Int, +: Int → Int → Int, string: Str, num: Num, int: Int, bool: Bool, unit: Unit, bot: ⊥, top: ⊤, Str class, Num class, Int class, Bool class, Unit class, Bot class, Top class, untyped class, Function class, Object class
//│ γ Set(Str, Int, κ Set(Str, Int))
//│ β Set(η Set(Str, Int), Str, Int)
//│ α Set(ζ Set(Str, Int), Str, Int)
//│ κ Set(Str, Int)
//│ List() IN κ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), γ ≤ κ, η ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), β ≥ η, ζ ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), α ≥ ζ, γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), γ fresh, β fresh, α fresh, κ fresh ||||| x: ι, θ fresh, b: η, η fresh, a: ζ, ζ fresh, foo: ∀α, β, γ ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. α → β → γ, ++: Str → Str → Str, -: Int → Int → Int, +: Int → Int → Int, string: Str, num: Num, int: Int, bool: Bool, unit: Unit, bot: ⊥, top: ⊤, Str class, Num class, Int class, Bool class, Unit class, Bot class, Top class, untyped class, Function class, Object class
//│ ι Set(κ Set(Str, Int, θ Set(Str, Int)), θ)
//│ γ Set(Str, Int, κ Set(Str, Int, θ Set(Str, Int)))
//│ β Set(η Set(Str, Int), Str, Int)
//│ α Set(ζ Set(Str, Int), Str, Int)
//│ κ Set(Str, Int, θ Set(Str, Int))
//│ θ Set(Str, Int)
//│ List(ι) IN θ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), κ ≤ θ, ι ≤ θ, ι ≥ κ, ι fresh, κ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), γ ≤ κ, η ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), β ≥ η, ζ ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), α ≥ ζ, γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), γ fresh, β fresh, α fresh, κ fresh, x: ι, θ fresh ||||| b: η, η fresh, a: ζ, ζ fresh, foo: ∀α, β, γ ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. α → β → γ, ++: Str → Str → Str, -: Int → Int → Int, +: Int → Int → Int, string: Str, num: Num, int: Int, bool: Bool, unit: Unit, bot: ⊥, top: ⊤, Str class, Num class, Int class, Bool class, Unit class, Bot class, Top class, untyped class, Function class, Object class
//│ γ Set(Str, Int, κ Set(Str, Int, θ Set(Str, Int)))
//│ β Set(η Set(Str, Int), Str, Int)
//│ α Set(ζ Set(Str, Int), Str, Int)
//│ κ Set(Str, Int, θ Set(Str, Int))
//│ θ Set(Str, Int)
//│ η Set(Str, Int)
//│ List() IN θ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), κ ≤ θ, κ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), γ ≤ κ, η ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), β ≥ η, ζ ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), α ≥ ζ, γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), γ fresh, β fresh, α fresh, κ fresh, x: ι, θ fresh, b: η, η fresh ||||| a: ζ, ζ fresh, foo: ∀α, β, γ ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. α → β → γ, ++: Str → Str → Str, -: Int → Int → Int, +: Int → Int → Int, string: Str, num: Num, int: Int, bool: Bool, unit: Unit, bot: ⊥, top: ⊤, Str class, Num class, Int class, Bool class, Unit class, Bot class, Top class, untyped class, Function class, Object class
//│ γ Set(Str, Int, κ Set(Str, Int, θ Set(Str, Int)))
//│ β Set(η Set(Str, Int), Str, Int)
//│ α Set(ζ Set(Str, Int), Str, Int)
//│ κ Set(Str, Int, θ Set(Str, Int))
//│ θ Set(Str, Int)
//│ η Set(Str, Int)
//│ ζ Set(Str, Int)
//│ List(γ, β, α, κ, θ, η, ζ) IN θ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), κ ≤ θ, κ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), γ ≤ κ, η ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), β ≥ η, ζ ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), α ≥ ζ, γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), γ fresh, β fresh, α fresh, κ fresh, x: ι, θ fresh, b: η, η fresh, a: ζ, ζ fresh ||||| foo: ∀α, β, γ ◁ {α ≤ (Int ▷ {γ ≥ Int, β ≤ Int}) ∨ (Str ▷ {γ ≥ Str, β ≤ Str}), β ≤ (Int ▷ {γ ≥ Int, α ≤ Int}) ∨ (Str ▷ {γ ≥ Str, α ≤ Str}), γ ≥ (Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})}. α → β → γ, ++: Str → Str → Str, -: Int → Int → Int, +: Int → Int → Int, string: Str, num: Num, int: Int, bool: Bool, unit: Unit, bot: ⊥, top: ⊤, Str class, Num class, Int class, Bool class, Unit class, Bot class, Top class, untyped class, Function class, Object class
//│ bop: ∀α, β ◁ {α ≥ ζ, α ≤ (Int ▷ {β ≤ Int}) ∨ (Str ▷ {β ≤ Str}), β ≥ η, β ≤ (Int ▷ {α ≤ Int}) ∨ (Str ▷ {α ≤ Str})}. ((Int ▷ {β ≤ Int}) ∨ (Str ▷ {β ≤ Str})) → ((Int ▷ {α ≤ Int}) ∨ (Str ▷ {α ≤ Str})) → ((Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str}))

bop(0, 1)
//│ FAILURE: Unexpected type error
//│ FAILURE LOCATION: raise (test.scala:53)
//│ ═══[ERROR] Type variable 'ζ' not found in the context.
//│ Typing error tree:
//│   ∀α, β ◁ {α ≥ ζ, α ≤ (Int ▷ {β ≤ Int}) ∨ (Str ▷ {β ≤ Str}), β ≥ η, β ≤ (Int ▷ {α ≤ Int}) ∨ (Str ▷ {α ≤ Str})}. ((Int ▷ {β ≤ Int}) ∨ (Str ▷ {β ≤ Str})) → ((Int ▷ {α ≤ Int}) ∨ (Str ▷ {α ≤ Str})) → ((Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})) ≤ Int → ν
//│     ζ ≤ α

bop("0", "1")
//│ FAILURE: Unexpected type error
//│ FAILURE LOCATION: raise (test.scala:53)
//│ ═══[ERROR] Type variable 'ζ' not found in the context.
//│ Typing error tree:
//│   ∀α, β ◁ {α ≥ ζ, α ≤ (Int ▷ {β ≤ Int}) ∨ (Str ▷ {β ≤ Str}), β ≥ η, β ≤ (Int ▷ {α ≤ Int}) ∨ (Str ▷ {α ≤ Str})}. ((Int ▷ {β ≤ Int}) ∨ (Str ▷ {β ≤ Str})) → ((Int ▷ {α ≤ Int}) ∨ (Str ▷ {α ≤ Str})) → ((Int ▷ {β ≤ Int, α ≤ Int}) ∨ (Str ▷ {β ≤ Str, α ≤ Str})) ≤ Str → ο
//│     ζ ≤ α
