:ctml

declare fun f: [A] -> ((A) -> A) -> List[A] -> A
//│ f: ∀A. (A → A) → List[A] → A

fun a1(x, y) = y
//│ a1: ∀30. ⊤ → 30 → 30
// OK

val a2 = choose(id)
//│ a2: ∀32. {32 ≥ ∀A. A → A} ⟹ 32 → 32
// OK

val a3 = choose(nil, ids)
//│ a3: List[∀A. A → A]
// OK

fun a4(x) = x(x)
//│ a4: ∀38, 39. {38 ≤ 38 → 39} ⟹ 38 → 39
// OK

val a5 = id(auto)
//│ a5: (∀A. A → A) → (∀A. A → A)
// OK

val a6 = id(auto2)
//│ a6: ∀B. (∀A. A → A) → B → B
// OK

val a7 = choose(id, auto)
//│ a7: (∀A. A → A) ∨ ((∀A. A → A) → (∀A. A → A))
// MAYBE

val a8 = choose(id, auto2)
//│ a8: (∀A. A → A) ∨ (∀B. (∀A. A → A) → B → B)
// MAYBE

val a9 = f(choose(id), ids)
//│ a9: (∀A. A → A) ∨ (∀A. A → A)
// MAYBE

val a10 = poly(id)
//│ a10: ⟨Int, Bool⟩
// OK

val a11 = poly((p) => p)
//│ a11: ⟨Int, Bool⟩
// OK

val a12 = id(poly((p) => p))
//│ a12: ⟨Int, Bool⟩
// OK

fun b1(f) = (f(1), f(true))
//│ b1: ((Int ∨ Bool) → ⊤) → ⟨⊤, ⊤⟩
// NOT OK

fun b2(xs) = poly(head(xs))
//│ b2: List[∀A. A → A] → ⟨Int, Bool⟩
// OK

declare fun g: [A] -> List[A] -> List[A] -> A
//│ g: ∀A. List[A] → List[A] → A

val c1 = length(ids)
//│ c1: Int
// OK

val c2 = tail(ids)
//│ c2: List[∀A. A → A]
// OK

val c3 = head(ids)
//│ c3: ∀A. A → A
// OK

val c4 = single(id)
//│ c4: List[∀A. A → A]
// OK

val c5 = cons(id, ids)
//│ c5: List[(∀A. A → A) ∨ (∀A. A → A)]
// MAYBE

val c6 = cons((x) => x, ids)
//│ c6: List[(∀128. 128 → 128) ∨ (∀A. A → A)]
// OK

val c7 = append(single(inc), single(id))
//│ c7: List[(Int → Int) ∨ (∀A. A → A)]
// MAYBE

val c8 = g(single(id), ids)
//│ c8: (∀A. A → A) ∨ (∀A. A → A)
// MAYBE

val c9 = map(poly, single(id))
//│ c9: List[⟨Int, Bool⟩]
// OK

val c10 = map(head, single(ids))
//│ c10: List[∀A. A → A]
// OK

val d1 = app(poly, id)
//│ d1: ⟨Int, Bool⟩
// OK

val d2 = revapp(id, poly)
//│ d2: ⟨Int, Bool⟩
// OK

val d3 = runST(argST)
//│ d3: Int
// OK

val d4 = app(runST, argST)
//│ d4: Int
// OK

val d5 = revapp(argST, runST)
//│ d5: Int
// OK

declare fun k: [A] -> (A,  List[A]) -> A
declare fun h: (Int) -> [A] -> (A) -> A
declare fun l: List[[A] -> (Int, A) -> A]
declare fun r: ([A] -> (A) -> [B] -> (B) -> B) -> Int
//│ k: ∀A. A → List[A] → A
//│ h: Int → (∀A. A → A)
//│ l: List[∀A. Int → A → A]
//│ r: (∀A. A → (∀B. B → B)) → Int

val e1 = k(h, l)
//│ e1: (Int → (∀A. A → A)) ∨ (∀A. Int → A → A)
// OK

val e2 = (k((x) => h(x)))(l)
//│ e2: (∀A. Int → A → A) ∨ (∀A. Int → A → A)
// OK

val e3 = r((x, y) => y)
//│ e3: Int
// OK

val f5 = auto(id)
//│ f5: ∀A. A → A
// OK

val f6 = cons(head(ids), ids)
//│ f6: List[(∀A. A → A) ∨ (∀A. A → A)]
// MAYBE

val f7 = head(ids, 3)
//│ f7: Int
// OK

val f8 = choose(head(ids))
//│ f8: ∀260. {260 ≥ ∀A. A → A} ⟹ 260 → 260
// OK

val f9 =
  let f = revapp(id)
  f(poly)
//│ f9: ⟨Int, Bool⟩
// OK

val f10 = choose(id, (x) => auto2(x))
//│ f10: (∀A. A → A) ∨ (∀276. (∀A. A → A) → 276 → 276)
// MAYBE

val g1a = z as ChurchInt
//│ g1a: ChurchInt
// OK

val g2 = s
//│ g2: ∀δ, ζ, ε, η. {δ ≤ η → ζ} ⟹ (δ → ε → η) → δ → (ε → ζ)
// MAYBE

// val g2a = s as ((ChurchInt) -> ChurchInt)
// NOT OK

// val g3a = n3 as ChurchInt
// NOT OK

:fixme
val g4a = ((u: Unit) => n3(n3)) as ((Unit) -> ChurchInt)
//│ ═══[ERROR] Type variable '298' not found in the context.
// NOT OK

val g5 = fst(fst(fst(n3((x) => (x, z), s(z)))))
//│ g5: ⊤ → ⊤ → (⊤ → ⟨⊤, ∀β. ⊤ → β → β⟩)
// MAYBE

val g7 = (s(s(z)))(s(s(z)))
//│ g7: ∀362, 366, 363, 364. ((366 → (362 ∧ 363)) ∧ ((363 ∨ 364) → 366)) → 364 → 362
// MAYBE

// def g8 = to_church
// TODO

// def g8a = to_church : ((Int) -> ChurchInt)
// TODO

// fun g9(x) = if true then x else g9(g9, x)
// TODO

// val g10 = ((x) -> x(x))((x) -> x(x))
// TODO

val g11 = auto(auto2(id))
//│ g11: ∀A. A → A
// OK

val g12 = ((y) =>
    let tmp = y(id)
    y(const)
) ((x) => x(x))
//│ g12: ∀420, 375, 424. {420 ≤ 420 → 375, 420 ≥ ∀A, B. A → ⊤ → A, 375 ≥ ⊤ → 420, 424 ≤ 424 → 425, 424 ≥ ∀A. A → A} ⟹ 375
// MAYBE

val g13 = ((k) => (k((x) => x), k ((x) => single(x)))) ((f) => (f(1), f(true)))
//│ g13: ⟨⊤, ⊤⟩
// NOT OK

val g14 = ((f) =>
    let a = ((u: Unit) => f(id))
    a(unit) as ((Int) -> ([B] -> (B) -> B))
) (const(const(id)))
//│ g14: ∀478. Int → 478 → 478
// OK

// Most of the "not simplified enough" types are due to bad check/constrain handling.
// A boolean flag is not good/enough, I should instead freeze context up to a certain level.
