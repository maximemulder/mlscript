:ctml

declare fun const: [A, B] -> A -> B -> A
//│ const: ∀A, B. A → B → A

val g12 = ((y) =>
    let tmp = y(id)
    y(const)
) ((x) => x(x))
//│ g12: ∀α, β, γ. {α ≤ α → β, α ≥ ∀A, B. A → ⊤ → A, β ≥ ⊤ → α, γ ≤ γ → δ, γ ≥ ∀A. A → A} ⟹ β
